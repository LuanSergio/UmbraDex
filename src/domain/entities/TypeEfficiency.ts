export default class TypeEfficiency {
  public name: string;

  public damageFactor: number;

  constructor(name: string, damageFactor: number) {
    this.name = name;
    this.damageFactor = damageFactor;
  }
}
