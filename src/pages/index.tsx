import Head from 'next/head';

const Home = (): JSX.Element => (
  <div>
    <Head>
      <title>UmbraDex</title>
    </Head>

    <main>
      <h1>UmbraDex</h1>
    </main>
  </div>
);

export default Home;
